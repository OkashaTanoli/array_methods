/*
 The includes method takes two arguments. The first is the value which we have to seacrh and
 the other is the index number from which it starts searching (by default it is 0).
 This method returns a boolean value if it finds the value it returns true otherwise false
*/

let arr = [4, 3, 2, 34, 20, 4, 2]

const result = arr.includes(3)

console.log(result)